language: ruby
cache: bundler
gemfile: Gemfile
before_install:
- gem install bundler
script:
  - bundle exec jekyll algolia --source _posts --destination _posts./site --config _config.yml
  #- bundle exec jekyll algolia --source _docs --destination _docs./_site --config docs/_config.yml
  - bundle exec htmlproofer ./_site --disable-external
branches:
  only:
    - master
rvm:
 - 2.4
